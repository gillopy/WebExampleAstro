---
---

<section id="inicio" class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text">
        <h1>Guillermo Cabrera</h1>
        <h2>Científico de Datos & Analista Estadístico</h2>
        <p>Transformando datos en insights accionables para impulsar decisiones basadas en evidencia</p>
        <div class="hero-btns">
          <a href="#proyectos" class="btn btn-primary">Ver Proyectos</a>
          <a href="#contacto" class="btn btn-outline">Contacto</a>
        </div>
      </div>
      <div class="hero-image">
        <div class="image-container">
          <svg class="bg-pattern" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
            <path fill="#3182CE" d="M41.3,-67.1C53.9,-60.3,64.5,-49.5,71.2,-36.5C77.9,-23.5,80.6,-8.3,77.8,5.9C75,20,66.6,33.2,56.6,44.5C46.6,55.9,35,65.5,21.6,70.1C8.3,74.7,-6.9,74.2,-20.9,69.5C-34.9,64.8,-47.7,55.9,-57.8,44.2C-68,32.5,-75.3,18,-76.7,2.8C-78.1,-12.5,-73.6,-27.6,-64.9,-39.7C-56.3,-51.7,-43.7,-60.5,-30.5,-66.9C-17.4,-73.3,-3.7,-77.3,9.3,-74.1C22.3,-70.9,28.7,-73.9,41.3,-67.1Z" transform="translate(100 100)" />
          </svg>
          <div class="profile-placeholder"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 9rem 0 5rem;
    position: relative;
    overflow: hidden;
  }

  .hero-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .hero-text {
    flex: 1;
  }

  .hero-text h1 {
    font-size: 3rem;
    margin-bottom: 0.5rem;
    background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-accent) 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-text h2 {
    font-size: 1.5rem;
    color: var(--color-secondary);
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .hero-text p {
    font-size: 1.1rem;
    color: var(--color-text-light);
    margin-bottom: 2rem;
    max-width: 500px;
  }

  .hero-btns {
    display: flex;
    gap: 1rem;
  }

  .hero-image {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .image-container {
    position: relative;
    width: 300px;
    height: 300px;
  }

  .bg-pattern {
    position: absolute;
    width: 100%;
    height: 100%;
    opacity: 0.3;
    animation: float 6s ease-in-out infinite;
  }

  .profile-placeholder {
    position: relative;
    width: 250px;
    height: 250px;
    border-radius: 50%;
    background-color: var(--color-accent-light);
    margin: 0 auto;
    z-index: 1;
    background: linear-gradient(135deg, var(--color-accent-light) 0%, var(--color-accent) 100%);
    box-shadow: 0 10px 30px rgba(49, 130, 206, 0.3);
  }

  @keyframes float {
    0% {
      transform: translateY(0px) scale(1);
    }
    50% {
      transform: translateY(-20px) scale(1.05);
    }
    100% {
      transform: translateY(0px) scale(1);
    }
  }

  @media (max-width: 768px) {
    .hero-content {
      flex-direction: column-reverse;
      text-align: center;
    }

    .hero-text h1 {
      font-size: 2.5rem;
    }

    .hero-text p {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-btns {
      justify-content: center;
    }

    .image-container {
      width: 250px;
      height: 250px;
      margin-bottom: 2rem;
    }

    .profile-placeholder {
      width: 200px;
      height: 200px;
    }
  }
</style>
